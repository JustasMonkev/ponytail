[package]
name = "ponytail-hook"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
